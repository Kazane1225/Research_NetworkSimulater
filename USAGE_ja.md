# 操作ガイド — 匿名動的ネットワークシミュレータ

このドキュメントでは、シミュレータのビルドと起動が完了した後の操作方法を説明します。
ビルド・起動方法については [README.md](README.md) を参照してください。

---

## 画面構成

ウィンドウはドラッグで移動できる縦型の仕切りで2つのパネルに分かれています。

| パネル | 説明 |
|--------|------|
| **左 — ネットワークパネル** | 動的ネットワークのグラフを表示します。エージェントがノード、現在のラウンドのインタラクション（有向辺）が赤い矢印で表示されます。 |
| **右 — 履歴ツリーパネル** | 全エージェントの履歴ツリー（ビュー）を表示します。各レベルが1ラウンドに対応し、有効なカウンティングアルゴリズムに応じてノードに色が付きます。 |

縦の仕切りを左右にドラッグするとパネルの幅を変更できます。

---

## ツールバー

ページ上部のツールバーから主要な機能にアクセスできます。

### ラウンド操作
| ボタン | ショートカット | 説明 |
|--------|----------------|------|
| ▲ | `↑` | 前のラウンドへ移動 |
| ▼ | `↓` | 次のラウンドへ移動 |
| + | `+` | 現在のラウンドの後に新しいラウンドを挿入 |
| − | `−` | 現在のラウンドを削除 |
| ⌫ | `Backspace` | 現在のラウンドの全リンクを削除 |

### アルゴリズム
| ボタン | 説明 |
|--------|------|
| **None** | アルゴリズムなし（デフォルト） |
| **Stabilizing** | 安定化カウンティングアルゴリズムを有効化 |
| **Terminating** | 終了保証カウンティングアルゴリズムを有効化 |
| ▶ Step | `Space` — 有効なアルゴリズムを1ステップ実行（選択が必要） |

有効なアルゴリズムのボタンがハイライト表示されます。**TAB** キーでも順番に切り替えられます。

### 編集
| ボタン | ショートカット | 説明 |
|--------|----------------|------|
| ✕ Agent | `Del` | 選択中のエージェントを削除 |
| ⊘ Deselect | `Esc` | エージェントの選択解除・ビュー選択のクリア |

### ファイル
| ボタン | ショートカット | 説明 |
|--------|----------------|------|
| 📂 Load | `L` | ファイルピッカーを開いて `.txt` ファイルからネットワークを読み込む |
| 💾 Save | `S` | 現在のネットワークを `Network.txt` として保存 |

### オプション（⚙）
**⚙ Options** をクリックすると表示トグルのポップオーバーが開きます：

| 項目 | ショートカット | 説明 |
|------|----------------|------|
| Toggle outdegree awareness | `O` | エージェントの出力次数認識の表示を切り替え |
| Toggle arrowheads | `A` | 辺の矢印の表示 / 非表示 |
| Toggle round / square nodes | `B` | ノードの形を丸 / 四角で切り替え |
| Toggle current-level highlight | `C` | 現在レベルのグレーハイライトバーの表示 / 非表示 |
| Cycle red-edge draw mode | `D` | 赤辺の描画モードを切り替え（全表示 → 選択ビューのみ → 非表示） |
| Snap agents to grid | `G` | 全エージェントをグリッドにスナップ |
| Two-way links by default (Caps Lock) | `⇪` | 有効時、リンクがデフォルトで双方向になる |

### ヘルプ（?）
**? Help** をクリック（または **H** キーを押す）すると、内蔵の操作一覧オーバーレイが開きます。

---

## はじめかた

1. `serve.bat` または `build-docker.bat` でアプリを起動し、ブラウザで **http://localhost:8000/** を開きます。
2. デフォルトのネットワークが自動的に読み込まれます。
3. ツールバーの **▲ / ▼** ボタン（または `↑` / `↓` キー、マウスホイール）でラウンドを移動できます。
4. ツールバーの **Stabilizing** または **Terminating** ボタン（または **TAB** キー）でアルゴリズムを選択します。
5. エージェントを選択した状態で **▶ Step** ボタン（または **スペース** キー）を押すとステップ実行できます。
6. **? Help** ボタン（または **H** キー）を押すといつでも操作一覧を表示できます。

---

## マウス操作

### 共通
| 操作 | 効果 |
|------|------|
| エージェントまたは履歴ノードを左クリック | 選択 / 選択解除 |
| 何もない場所を左クリック | 現在の選択を解除 |
| マウスホイール上 | 前のラウンドへ |
| マウスホイール下 | 次のラウンドへ |

### ネットワークパネル
| 操作 | 効果 |
|------|------|
| エージェントを左クリックしてドラッグ | そのエージェントから有向リンクを引く |
| エージェントを右クリックしてドラッグ | エージェントを移動する |
| エージェント以外の場所を右クリック | その位置に新しいエージェントを作成 |

#### リンクを引く／削除するときの修飾キー
| 押しているキー | 効果 |
|----------------|------|
| **Ctrl** または **Q** | リンクを作成する代わりに削除する |
| **Shift** | 双方向リンクにする |
| **Alt** | 現在のラウンドだけでなく **全ラウンド** に変更を適用する |
| **Caps Lock**（トグル） | デフォルト方向を反転：双方向がデフォルトになり、Shift で一方向になる |

### 履歴ツリーパネル
| 操作 | 効果 |
|------|------|
| ノードを左クリック | そのノードが属する匿名性クラスを選択 / ハイライト |

---

## キーボードショートカット

### ナビゲーション
| キー | 操作 |
|------|------|
| `↑` / `↓` | 前のラウンド / 次のラウンドへ移動 |
| `←` / `→` | 現在のビューで前 / 次の匿名性クラスを選択 |
| `ESC` | エージェントの選択を解除し、ビューの選択もクリア |

### ネットワーク編集
| キー | 操作 |
|------|------|
| `DEL` または `U` | 選択中のエージェントを削除 |
| `+` | 現在のラウンドの後に新しいラウンドを挿入 |
| `-` | 現在のラウンドを削除 |
| `Backspace` | 現在のラウンドの**全リンク**を削除 |
| `0`〜`9` | 選択中のエージェントの**入力値**を設定（0 = リーダー `L`） |
| `O` | エージェントの出力次数認識（outdegree awareness）を切り替え |
| `G` | 全エージェントをグリッドにスナップ |
| `L` | ファイルからネットワークを読み込む |
| `S` | 現在のネットワークをファイルに保存 |

### 表示オプション
| キー | 操作 |
|------|------|
| `A` | 辺の矢印の表示 / 非表示を切り替え |
| `D` | 赤辺・出力次数の描画モードを切り替え（全表示 → 選択ビューのみ → 非表示） |
| `B` | ノードの形を四角 / 丸で切り替え |
| `C` | 履歴ツリーの現在レベルにグレーのハイライトバーを表示 / 非表示 |

### アルゴリズム
| キー | 操作 |
|------|------|
| `TAB` | カウンティングアルゴリズムを順に切り替え：**なし → 安定化 → 終了保証 → なし** |
| `スペース` | 有効なアルゴリズムを1ステップ実行（エージェントまたは履歴ノードの選択が必要） |

### その他
| キー | 操作 |
|------|------|
| `H` | 操作一覧オーバーレイの表示 / 非表示 |

---

## カウンティングアルゴリズム

**TAB** キーでアルゴリズムを切り替えます。切り替え時に画面上にメッセージが表示されます。

### アルゴリズムなし（デフォルト）
色付けなしで履歴ツリーを描画します。

### 安定化カウンティングアルゴリズム
履歴ツリーの各可視ノードが以下の色で表示されます：
- **黄色** — まだ推定されていない
- **緑** — 正しく推定された（推定値が真の匿名性に一致）
- **赤** — 誤った推定

### 終了保証カウンティングアルゴリズム
追加の色状態：
- **黄色** — まだ推定されていない
- **シアン** — 初期レベルの推定済みノード
- **オレンジ** — 推定済み（中間状態）
- **緑** — カウント完了（最終的な正解）

#### ステップ実行
アルゴリズムが有効な状態でエージェントまたは履歴ノードを選択し、**スペース** キーを繰り返し押すと1ステップずつ実行されます。現在のステップ番号が画面に表示されます。

---

## エージェントの入力値

各エージェントのノード内に入力値が表示されます：

| 表示 | 意味 |
|------|------|
| `L` | リーダー（入力 = 0）。リーダーは常にリストの先頭に並びます |
| `1`, `2`, … | 対応する入力値を持つ非リーダーエージェント |

エージェントを選択した状態で数字キー **0〜9** を押すと入力値を変更できます。`0` はそのエージェントをリーダーにします。

---

## ネットワークの読み込みと保存

### 保存
**S** キーを押すと現在のネットワークを保存します。ファイル名は `Network.txt` になります。

### 読み込み
**L** キーを押すとファイルピッカーが開き、`.txt` ネットワークファイルを読み込めます。

### ファイル形式
ネットワークファイルは以下の構造を持つプレーンテキストです：

```
entity (<入力値>, <x>, <y>)
...
round <n>
inter (<送信元>, <送信先>, <多重度>)
...
```

- `entity` 行：エージェントを定義します。**入力値**（0 = リーダー）、**x/y** のワールド座標。
- `round` ヘッダー：インタラクションをラウンド番号（1始まり）でグループ化します。
- `inter` 行：有向インタラクションを定義します。**送信元**インデックス、**送信先**インデックス、**多重度**。

`networks/` ディレクトリにはすぐに読み込めるサンプルファイルが多数収録されています。

---

## 収録サンプルネットワーク

`networks/` フォルダには様々なトポロジのサンプルが含まれています。主なものを以下に示します：

| ファイル | 説明 |
|----------|------|
| `TerminatingWorstCase.txt` | 終了保証アルゴリズムの最悪ケース |
| `LowerBound2n.txt` | 約 2n ラウンドを必要とする下界構成 |
| `LowerBound3n2.txt` | 約 3n/2 ラウンドを必要とする下界構成 |
| `BoldiVigna.txt` | Boldi–Vigna の二部匿名ネットワーク |
| `Circus.txt` | サーカス型トポロジ |
| `Sunflower.txt` | ひまわり型トポロジ |
| `StabilizingCounterexample*.txt` | 単純な安定化アプローチへの反例 |

---

## 参考文献

理論的背景については以下を参照してください：  
<https://arxiv.org/abs/2404.02673>
