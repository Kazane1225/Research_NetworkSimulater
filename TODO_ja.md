# TODO

## 優先度：高

### 1. `MergeHistoryTrees` のノードハッシュ化
**ファイル:** `src/history_tree.h`, `src/history_tree.c`

`HistoryTree` 構造体に `hash` フィールドを追加し、マージのたびにボトムアップで計算する。ハッシュにはサブツリー全体の構造（input、outdegree、子ノードのハッシュ、赤辺のターゲットハッシュ＋multiplicity を並べ替えたもの）を反映させる。

`MergeHistoryTrees` と `HistoryTreeContains` の子ノード探索内ループで `EquivalentNodes`（O(obs)）の代わりにハッシュ値比較（O(1)）を使う。`b->children` にハッシュをキーとしたルックアップテーブルを持たせることで、全体のコストを O(C × obs) から O(C) または O(1) に削減できる。

期待される効果：マージ呼び出しあたり最大 **~obs 倍** 高速化（obs は最大 n−1）。

---

### 2. ラウンド追加・削除時のインクリメンタル更新
**ファイル:** `src/network.c`, `src/entity.c`

現在 `ExecuteNetwork()` はラウンドが変わるたびに全エンティティのヒストリーツリーをゼロから再構築している。以下のように変更する：
- **ラウンド追加時**：既存ツリーの上に新しいラウンドだけを差分適用する。
- **ラウンド削除時**（末尾のラウンドのみ）：そのラウンドを適用する前のツリー状態のスナップショットを保持しておき、ロールバックする。

これにより、ユーザー操作のたびに発生していた O(rounds × n²) の処理を削減できる。

---

## 優先度：低

### 3. `HistoryTree` ノードのアリーナアロケータ化
**ファイル:** `src/history_tree.c`

ノードごとの `malloc`/`free` を、ネットワーク実行のライフタイムに紐づいたスラブ／アリーナアロケータに置き換える。ラウンド数が多い場合にアロケータのオーバーヘッドを削減できる。

### 4. `HistoryTreeEquals` のハッシュによる早期終了
ノードハッシュが実装済み（上記 1 完了後）であれば、`HistoryTreeEquals` でまずルートのハッシュを比較し、異なれば即座に `false` を返すことで、2回の BFS 全走査を回避できる。
